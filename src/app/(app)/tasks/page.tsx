
"use client"; // Ajouté pour s'assurer que c'est un Client Component si AiTaskManager l'est

import AiTaskManager from '@/components/thesis/AiTaskManager'; // Modifié ici

export default function TasksPage() {
  return <AiTaskManager />;
}
